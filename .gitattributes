# Normalize all text files to LF in the repository and on every platform.
# Prevents CRLF/LF churn from mixed Windows/WSL checkouts.
* text=auto eol=lf

# Windows scripts that genuinely need CRLF stay explicit.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
